# File: required_vars.yaml
# Required environment variables for Sovereign Stack validation
# Version: 1.0.0 (Synced with verify_env.sh 2026-04-05)

global_settings:
    - TZ
    - DOMAIN
    - SECOND_DOMAIN
    - DOCKER_ROOT
    - INTERNAL_HOST_IP

infrastructure:
    - EXTERNAL_DNS_IP
    - EXTERNAL_DNS_NAME
    - NPM_CERT_ID
    - SIGNUPS_ALLOWED
    - COTUR_SECRET

security_identity:
    - STEP_CA_EMAIL
    - STEPCA_PASSWORD
    - STEPCA_PROVISIONER_NAME
    - STEPCA_PROVISIONER_PASSWORD
    - STEP_CA_DNS_IP
    - STEPCAT_FINGERPRINT

backup_system:
    - BACKUP_EMAIL
    - BACKUP_ENCRYPTION_KEY
    - BACKUP_LOCAL_TARGET
    - BACKUP_LOCAL_RETENTION_DAYS

backup_offsite:
    - BACKUP_OFFSITE_IP
    - BACKUP_OFFSITE_USER
    - BACKUP_OFFSITE_PASSWORD
    - BACKUP_OFFSITE_PATH
    - BACKUP_OFFSITE_OS
    - BACKUP_OFFSITE_WOL
    - BACKUP_OFFSITE_MAC
    - BACKUP_OFFSITE_MAX_RETRIES
    - BACKUP_OFFSITE_RETRY_WAIT

matrix_vloot:
    - SYNAPSE_NAMES
    - SYNAPSE_VMS
    - SYNAPSE_REMOTE_PATHS
    - SYNAPSE_USER
    - SYNAPSE_DB_CONTAINER
    - SYNAPSE_DB_USER
    - SYNAPSE_DB_PASSWORD

nextcloud:
    - INCLUDE_NEXTCLOUD_DATA
    - NEXTCLOUD_MAIL_FROM_ADDRESS
    - NEXTCLOUD_MAIL_DOMAIN
    - NEXTCLOUD_MAIL_SMTPHOST
    - NEXTCLOUD_MAIL_SMTPPORT
    - NEXTCLOUD_MAIL_SMTPSECURE
    - NEXTCLOUD_MAIL_SMTPAUTH
    - NEXTCLOUD_MAIL_SMTPNAME
    - NEXTCLOUD_MAIL_SMTPPASSWORD
    - NEXTCLOUD_DB_ROOT_PASSWORD
    - NEXTCLOUD_DB_PASSWORD
    - NEXTCLOUD_DB_USER
    - NEXTCLOUD_DB_NAME
    - NEXTCLOUD_TRUSTED_DOMAINS

forgejo:
    - FORGEJO_USER
    - FORGEJO_DB_USER
    - FORGEJO_DB_PASSWORD
    - FORGEJO_DB_NAME

netbox:
    - NETBOX_API_TOKEN_PEPPERS
    - NETBOX_SECRET_KEY
    - NETBOX_DB_NAME
    - NETBOX_DB_USER
    - NETBOX_DB_PASSWORD
    - NETBOX_ALLOWED_HOSTS
    - NETBOX_URL
    - NETBOX_API_TOKEN

frigate_ha:
    - INCLUDE_FRIGATE_DATA
    - FRIGATE_RTSP_PASSWORD
    - FRIGATE_MQTT_USER
    - FRIGATE_MQTT_PASSWORD
    - HA_USER
    - HA_PASSWORD
    - HA_MQTT_USER
    - HA_MQTT_PASSWORD

signal_communication:
    - SIGNAL_PASS
    - SIGNAL_URL
    - SIGNAL_SENDER
    - SIGNAL_RECIPIENT
    - SIGNAL_CLI_API_PASSWORD
    - SIGNAL_API_PORT

storage_s3:
    - GARAGE_RPC_SECRET
    - GARAGE_ROOT

misc_ui:
    - ICON_BLACKLIST_LOCAL
    - ICON_DOWNLOAD_TIMEOUT

unifi:
    - UNIFI_DB_PASSWORD

bpt_meldingen:
    - BPT_DB_PASSWORD
    - BPT_SECRET_KEY
    - BPT_REGISTRATION_TOKEN
